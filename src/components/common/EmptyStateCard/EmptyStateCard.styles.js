import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    borderRadius: 16,
    padding: 24,
    alignItems: 'center',
    marginTop: 24,
    borderWidth: 1,
    borderColor: '#dfe7f4',
  },
  title: {
    fontSize: 18,
    fontWeight: '700',
    color: '#fff6ea',
    textAlign: 'center',
  },
  message: {
    marginTop: 8,
    fontSize: 14,
    color: '#cdbf9a',
    textAlign: 'center',
  },
});

export default styles;
