pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven {
            url = uri('../node_modules/react-native/gradle-plugin')
        }
    }
}

rootProject.name = "TKO_APP"

include ':app'